export const metadata = {
    title: "join",
    description: "jion",
}

export default function JoinLayout({ children }) {
    return (
        <div className="wrapper my-16">
            {children}
        </div>
    );
}